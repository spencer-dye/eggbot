# `@eggbot/policy`

The mandatory boundary between proposed decisions and executable actions. Rules are deterministic and return inspectable rejection reasons. Evaluation performs no platform writes.
