# `@eggbot/policy`

The mandatory boundary between proposed decisions and executable actions. Rules are deterministic and return inspectable rejection reasons. Evaluation reports approval independently for every action and retains every rule violation; orchestration must explicitly derive approved actions. Evaluation performs no platform writes.
