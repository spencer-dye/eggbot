# `@eggbot/core`

Platform-independent fantasy-football vocabulary, observed standings and transactions, and inspectable action data. This package deliberately contains no transport, provider, storage, or model integration.

All IDs are opaque branded strings. Convert untrusted strings with the exported ID constructors at system boundaries.
