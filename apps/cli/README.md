# `@eggbot/cli`

A deliberately small Phase 0 composition proof. It imports the public workspace packages, wires a deterministic no-op decision engine to an empty policy boundary, and reports that Yahoo remains unavailable. It performs no network, persistence, scheduling, or roster side effects.
